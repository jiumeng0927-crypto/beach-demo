# 近岸泡沫与沙地细节：0.40.0

项目：潮汐沙岸。学习者：钱奕好。日期：2026-09-12。

本轮继续改善现有近景，没有更改海岸分区、模型地标、台球规则或 NPC 玩法。沿用 0.39 的 HDR 天空、单层水体、材质和素材许可，不新增下载依赖。

## 1. 泡沫与沙地必须使用相同网格

旧泡沫用独立、较粗的高度采样网格。即使每个顶点都在对应解析地形上方 0.025，两个网格三角形的内部插值仍然不同，会出现相交、缺口或碎片。

`buildFoamGeometry(beachGeometry)` 现在直接复制沙地前部顶点，并保留相同的三角形对角线；不再重复调用高度公式生成另一套地形。水平位置一一对应，静态垂直偏移固定为 0.025。顶点着色器再把水下部分抬到实际潮位之上，干地部分继续贴地。泡沫不随潮位整片平移。

高/低档泡沫分别为 19,800 / 9,300 个三角形，只覆盖近岸区域；它在水面反射与折射捕获中继续隐藏。成本比旧泡沫增加，移动端必须保留原 220,000 个场景三角形预算，不能用“只增加一个网格”掩盖成本。

## 2. 推进、破碎与回退

`createFoamMaterial()` 将一个周期分成推进和回退，浪锋目标深度在 0.26 与 -0.035 之间平滑变化。沿岸低频扰动让不同位置略微错开，周期首尾用连续透明度淡出，避免整条线突然跳回。

浪锋是较密的窄条泡沫，后侧水膜用细胞边缘和多尺度噪声产生断续残留。初版网纹太整齐，像裂纹，随后缩小细胞、增加局部断裂并降低残留透明度。纹理使用世界坐标，不会跟着相机滑动；屏幕导数给细线留抗锯齿宽度。

所有泡沫变化统一使用 `uWavePhase`，不再一部分跟水面时钟、一部分跟真实经过时间。波速为零时，浪锋和破碎细节一起停止。原泡沫强度、潮位及昼夜色调控制继续有效。材质接入 Three.js 的雾效，ShaderMaterial 需显式提供 `UniformsLib.fog`，仅设置 `fog: true` 不足以完成配置。

这是跟随潮位的程序化冲刷外观，并不是流体求解、真实卷浪或泡沫粒子的质量守恒模拟。水面几何浪和冲刷周期共用时间，但尚未按实际浪峰到岸速度逐波匹配。

## 3. 草叶变黑的原因

Three.js 同时启用顶点色和实例色时，两者会相乘。旧草叶已有根部到叶尖的深绿色顶点色，又乘了深绿色实例调色，造成二次压暗。

现在由顶点色负责叶片本色与根尖渐变，实例色只作接近白色的轻微冷暖变化。其线性通道范围约为 0.665 到 1，不再用 0.074 到 0.279 的暗色重复染色。叶片缩窄并向外弯曲，高档采用三段、低档保留两段，实例数量仍为 460 / 200。略微补充背面透光近似，夜间不额外发亮。

变宽的草丛包围盒继续经过既有器材/栈道避让逻辑；不修改随机数调用顺序，不挪动器材来迁就草叶。现有扫描模型和所有角色资源不变。

## 4. 沙地法线的稳定性

近景隔离实验显示：关闭阴影后细碎亮线仍在，关闭程序凹凸后消失。因此修正在材质法线计算，不靠删阴影或降低整个场景对比度遮掩。

旧实现把三个近似高度值相减再除以很小的间隔。现在对五次平滑插值的噪声直接求解析梯度，避免相近浮点值相减，并对旋转噪声应用正确的梯度变换。微小颗粒和细波在小于像素尺寸时逐渐衰减，保留近处细节，降低远处闪烁。

微观坡度叠加到原地形法线，不把坡地逐渐混向世界向上方向；保留 Three.js 的 `nonPerturbedNormal` 作为几何粗糙度输入，不再将它覆盖为凹凸后的法线。高档执行这一层，低档继续使用较轻的色彩/粗糙度流程。

参考：[Three.js MeshStandardMaterial](https://threejs.org/docs/pages/MeshStandardMaterial.html)、[GPU Gems 多尺度水面与解析导数](https://developer.nvidia.com/gpugems/gpugems/part-i-natural-effects/chapter-1-effective-water-simulation-physical-models)。具体材质插入点以锁定的 Three.js r160 源码为准。

## 5. 检查与练习

```bash
npm test
npm run test:swash
npm run test:shore-detail
npm run build
npm run test:production
npm run test:visual
```

`test:swash` 使用桌面 1440×900 和手机 390×844，拍摄低/高潮位、多段波相位及草叶近景。将泡沫单独渲染到黑底，检查真实像素运动；固定水面时间但推进通用时间，要求泡沫像素完全不变。还逐点检查沙地贴合、三角形一致性、实例染色及切档后的单次释放。图片输出为 `screenshots/shore-swash-040-after-*.png`。

练习时分别改变浪锋宽度、残留透明度和细胞尺寸，并使用同相机对比。不要只拍一个最好看的时刻：过于规则的网纹、周期跳变和移动端亚像素闪烁，都可能在静态截图中被漏掉。旧版对照保留在 `before` 文件中，不能用新画面覆盖。
