# 俯身台球、碰撞声音与球桌细化

版本：0.42.0。项目成员：钱奕好。更新日期：2026-09-13。

## 范围与操作

保留沙滩布局、水体、植被、游客、三章拾取及连续昼夜。中八规则、球径、有效台面、六段边库物理体、十二个袋角、袋口宽度和原台呢摩擦不变。

1. 点击台球定位或准星图标，进入白球中心的低位瞄准视角；方框图标切回俯视。
2. 在画面空白处左右拖动调整方向，向下拉杆蓄力、松手击球。仅横向调整或轻点不会出杆；取消触摸、切换相机、窗口失焦会取消蓄力。
3. 也可以用左右箭头每次微调 0.25 度，调节力度滑块并点击“击球”。原生按钮和滑块支持键盘操作。
4. 俯视模式保留按住白球向后拖拽击球。自由球和开球裁定自动进入俯视，确认落点后回到俯身视角。
5. 喇叭按钮独立控制台球声音，“杆法与台呢”面板可调音量。高低杆与左右塞仍使用原物理控制，并同步到可见杆头的位置。

手机瞄准时图鉴面板暂时隐藏，球局面板移到下方；退出后恢复原布局，不清空探索记录。

## 相机和球杆

`BeachBilliardsGame.getCueViewPose()` 返回白球世界位置作为 target。镜头在其后方 1.65 单位，手机为 1.85，抬高 0.48 单位；方向来自 `viewYaw`，不再固定围绕球桌中心。

`BeachExperience` 持有视角状态并沿用现有补间。瞄准模式停止 OrbitControls 更新，避免其 8 单位最小距离把镜头重新拉远。击球后不追逐高速白球，保持出杆位置观察结果；球静止后再平滑定位。返回沙滩、切换自由视角或恢复书签会退出专用瞄准模式。

杆头、先角、木质前节、连接环、握把和尾垫合并为一个网格。拉杆时后撤，杆头随高低杆和左右塞偏移。接近身后边库时，视觉球杆会抬高以减少穿库。**抬杆仅为视觉避让，不新增跳球、弧线或球杆刚体碰撞**，物理仍使用既有接触点冲量。

## 真实事件与声音

`BilliardsAudio` 按脚步声音模块的按需创建方式独立管理 AudioContext。首次点击台球相关操作才创建/恢复音频上下文；默认允许台球声音，但不会在页面加载时自动播放。

- 击杆：成功施加白球冲量后触发，拒绝的击球不发声。
- 球碰球：Cannon 的新接触对触发，强度来自接触约束冲量估算。
- 碰库：使用真实边库接触，音色比球碰球低沉。
- 落袋：进入物理捕获区时触发，先读取速度再移除球体。

四种短音均由阻尼谐振与短噪声瞬态本地合成，不下载音频、不使用无授权采样。球碰球更短、更亮，边库和落袋使用低频衰减。强度随碰撞改变，声像按相机右方向投影，距离越远越弱。这是轻量立体声近似，没有真实混响、遮挡传播或录音级音色还原。

接触对去重避免同一持续接触每个 1/240 秒重复触发。最多同时保留 12 个声音，主总线压缩器控制开球叠加的动态。声音结束即断开节点；静音、隐藏页面、重置和销毁会停止活动节点。不可用或未获浏览器解锁的音频不阻断游戏，也不补播积压碰撞。

## 球桌建模

木框从直角盒子改为倒角实例，增加六点支撑、横梁、调平脚、金属螺栓与包边；边库外侧增加十八个定位菱形。木纹为本地生成纹理。皮革袋圈和下垂袋体为合并网格，不扩大袋口。

边库上缘增加斜面，白球中心高度处的库鼻仍与原物理边界一致；射线测试检查六段库鼻的渲染位置和朝外法线。台呢去掉容易产生摩尔纹的规律条纹，使用低对比颗粒，台板侧壁加深。

常驻三维绘制对象由 5 增到 8：床面、木框、袋底、球、瞄准线，加五金、皮袋、球杆。普通观察实际显示 6 个，瞄准时增加球杆或瞄准线。不增加球/边库刚体数量。新增几何、材质与纹理按原生命周期回收。

## 验证与练习

```bash
npm test
npm run test:billiards-presentation
npm run test:billiards-input
npm run test:visual
npm run build
npm run test:production
```

专项覆盖四类波形的有限值/峰值/均方根、音频解锁失败、并发上限、静音与单次销毁；72 种白球位置/方向组合；六段可见库鼻与物理边界一致；真实鼠标/触摸击球、轻点不出杆、白球居中、击球镜头固定、自由球切换；浏览器实时音频输出与离线渲染波形；桌面/手机截图及新增资源单次释放。

截图见 `screenshots/billiards-042-*`。手机检查是本机浏览器的触屏视口模拟，不代表所有实体手机的帧率。

### 本轮实测

生产构建 `index-xuEUXtSH.js`：20 组确定性测试、11 组生产浏览器专项及根路径/仓库子路径/HDR 加载失败回退测试全部通过。88 个 JS/MJS 文件语法检查通过，依赖审计未发现已知漏洞。GitHub Actions 配置已更新，但未上传仓库或执行远程 CI。

完整画面采样为桌面 126 次绘制、581738 个三角形，手机视口 68 次绘制、259561 个三角形；两者均无控制台错误和布局溢出，仍在上一版预算内。本轮没有通过提高预算来放宽验收。

实时音频图检测到非零且未削波的输出，离线四类声音的均方根约为 0.091–0.112。鼠标和触摸开球均触发击杆、球碰球和碰库；专项中的真实物理进球触发落袋声，重复收取同一球不会重复响。新增三组几何/材质与木纹纹理均验证单次释放，音频上下文最终关闭。

相机、台面和音效属于实时教学近似，不构成赛事模拟器或录音级音质承诺。

练习：给球碰球增加轻重两种音色，保留同一接触判定和并发限制，再用固定力度击球比较波形与事件数，避免物理接触和界面按钮各播放一次。

参考：[MDN Web Audio 最佳实践](https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API/Best_practices)、[Cannon ContactEquation](https://pmndrs.github.io/cannon-es/docs/classes/ContactEquation.html)、[Three OrbitControls](https://threejs.org/docs/pages/OrbitControls.html)。具体实现以项目锁定版本为准。
