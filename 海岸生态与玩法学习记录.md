# 海岸生态、图鉴与连续时间

版本：0.41.0。项目成员：钱奕好。

## 这一版的范围

保留海岸地形、木船、栈道、器材与休息区布局；中八规则和物理不变。更新草、棕榈、海鸥、两位现有游客，并扩展拾取任务与天空时间。旧 Q 版参考图和实验角色仍不进入网页。

## 模型细节与成本

- 草：每片叶子由左右两侧和中脊构成，形成真实折面，沿高度收窄并弯曲。高档每丛 60 三角形，低档 40；仍为 460/200 个实例，并继续按器材和步道包围盒让开。
- 棕榈：树干每节沿真实弯曲方向拼接，将原先 0.72 的突兀锥缩改为 0.967，增加表皮环纹。羽状叶有中脊、两侧弯折和下垂尖端。高档 21 段，低档 14 段；根部位置和树冠数量不变。
- 海鸥：原来是线段，现在使用身体、头、喙、眼、尾羽与左右翼羽网格。沿闭合椭圆巡航，分批拍翼与滑翔，不再从画面一侧突然传送到另一侧。三批实例绘制，桌面 6 只、手机 3 只；左翼镜像后修正索引绕序。
- 游客：在 Quaternius 原始骨骼模型上做一级细分、减面和光滑法线，补充帽子、收集包、背带、墨镜和帆布鞋。保留 Idle、Walk、Victory 原动画，每人仍合并为一种顶点色材质。导出模型分别约 1.29 万三角形，不是写实扫描人物。

源码：`world.js`、`CoastalGulls.js`、`prepare-npc-assets.py`。初次细分版本过重，已减面并为手机棕榈降低细分。整帧预算包含水面反射/折射：桌面 65 万三角形、180 次绘制；手机 27 万、96 次绘制。不是承诺所有手机都能达到固定帧率。

重新导出游客需要 Blender 4.5，使用人物专用的下载和导出命令：

```bash
node scripts/fetch-npc-assets.mjs
npm run prepare:npcs
npm run check:assets
```

下载脚本和导出脚本均校验原始文件 SHA256，导出后同步派生 GLB 哈希。原始模型许可为 CC0，新增配件与程序为 MIT，详见第三方声明。未改动用户原有压缩包。

## 三章拾取

1. 海玻璃标本：原六处地标保留。
2. 退潮贝壳：六枚空贝壳，前三枚要求潮位 `level <= -0.22`，高潮时不会显示或接受拾取。
3. 海岸清理：收集六只废弃瓶罐，完成后获得第三枚徽记。

合计 18 处。每章完成后，图鉴或小林的对话可以开启下一章；不是自动瞬间刷新同一批物体。不同章节使用海玻璃、带肋贝壳、旋转成型瓶身几何。图鉴记录线索、章进度和徽记，小林会提示当前可拾取的位置；余下物品被潮水覆盖时改为提示等待退潮。

`CollectionCampaign` 是不依赖 Three 的任务状态机，`BeachDiscoveryGame` 负责拾取检测和三维反馈，`CollectionJournal` 负责图鉴。拾取是幂等操作，不能重复计数；不能跳过未完成章节；射线检测会检查地形/场景遮挡。仍支持鼠标、触摸和近距离 E。

状态写入 `localStorage` 的 `tideline.collection.v1`。只保存版本、合法物品 ID、章节与领奖状态，不保存任意执行内容。损坏/不可用存储不会阻断网页，图鉴会显示是否已保存在本机。刷新可继续，但不跨设备同步；完成后的“重新探索”会重置这份存档。

每次只保留当前章节的一个 InstancedMesh 和一组微光 Points。换章释放旧几何/材质，不累计 18 套模型。画质切换重绑新 world，保持收集进度和 NPC 对话状态。

## 连续时间和动态云

默认从 12:00 开始，以真实可见运行时间推进：现实 30 分钟约等于场景一昼夜。可取消“时间流逝”固定时刻，四个时段按钮仍可直接定位。页面隐藏后暂停，不在回到页面时突然跨过一整晚；不读取真实天气或联网气象数据。

`CoastalClock` 在清晨、日光、黄昏和月夜间连续插值，跨午夜正确取模，方位角走连续路径。物理仍使用原有受限 delta；时间使用实际帧间隔，低帧率不让时钟持续变慢。灯光、雾、沙色和水色沿用同一份 atmosphere；PMREM 最多每 20 秒刷新一次。

可见天空不再显示 HDR 内固定不动的云。HDR 仍作为 PBR 环境光来源；独立程序化云穹顶负责实时漂移、密度演变、侵蚀边缘和向光明暗。风驱动漂移，独立 `uEvolution` 驱动云形变化，因此无风也不会完全静止。画质切换时复制两个时钟，避免云形回到初始状态。

这是连续更新的天空着色器，不是体积光线步进，也不是气象流体模拟。云与物体间未实现精确投影阴影，HDR 环境光中的云形与实时云穹顶不是逐像素相同。

## 验证与练习

```bash
npm test
npm run test:living-coast
npm run test:swash
npm run test:ocean-npc
npm run test:visual
npm run build
npm run test:production
```

新增测试覆盖章节解锁、潮位限制、重复拾取、存档续玩、损坏/禁用存储、午夜连续性、帧率无关时间、桌面/手机图鉴、云形像素变化、海鸥运动和资源单次释放。实际测试截图存放在 `screenshots/living-coast-041-*`。

### 本轮实测结果

2026-09-13，生产构建 `index-CIPQGqmP.js`：19 组确定性测试、10 组生产浏览器测试及根路径/子路径/HDR 加载失败回退测试均通过；84 个 JS/MJS 文件语法检查通过，依赖审计未发现已知漏洞。GitHub Actions 配置已更新，但尚未上传仓库或运行远程 CI。

完整画面回归采样：桌面 124 次绘制、574850 个三角形；手机视口 66 次绘制、254497 个三角形。两者没有控制台错误和布局溢出，跟踪资源按预期释放。这是本机浏览器的视口模拟结果，不代表所有实体手机的性能。

实际鼠标/触摸拾取、三章完成、本机存档刷新续玩均通过；旧水体残影、岸线、场景碰撞和台球输入回归保留。关闭风漂移后，仅推进云形演变 120 秒，桌面/手机采样区域分别约有 7.13%/8.79% 像素变化，确认云不是只做整层平移。

练习：给一枚贝壳增加昼夜条件时，先在纯状态机里定义条件并写测试，再让三维显示与提示文字共用同一个判断，避免“看得见却捡不到”或“看不见还能被点击”。

参考：[Three InstancedMesh](https://threejs.org/docs/pages/InstancedMesh.html)，[Quaternius 原始模型](https://quaternius.com/packs/ultimatedanimatedcharacter.html)。具体 API 以项目锁定的 Three r160 与 Blender 4.5 为准。
