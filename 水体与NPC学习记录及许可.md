# 水体重写与海滨 NPC

版本：0.41.0。学习者：钱奕好。项目：潮汐沙岸。

0.41 细化了两位游客的模型和服饰配件，对话任务扩为三章 18 处。当前 NPC 制作、存档和动态云/时间见 [生态与玩法学习记录](LIVING_COAST_041.md)。水体深度修复保持不变。

0.40 的泡沫网格贴合、冲刷分层、草叶色彩和沙地解析梯度，见 [近岸细节学习记录](NEAR_SHORE_FINISH_040.md)。场景分区与玩法保持不变。

0.39 的 HDR 天空、光学分层、薄水层边缘修复、素材校验与同镜头视觉实验，见 [水体质感学习记录](WATER_QUALITY_039.md)。以下保留逐版学习过程，历史参数不等同于当前配置。

## 0.38：潮位、礁石和草丛的衔接

本轮保持 0.37 的场景分区，不增加角色或改变台球规则。重点是消除近景穿插，让已有材质与潮位连起来。

### 岩石湿润带

`CoastalStyle.js` 中的 `TIDAL_WETNESS_GLSL` 按世界空间高度减去潮位计算湿润程度。水线上方约 0.02 至 0.38 场景单位柔和过渡，小幅空间扰动避免笔直色带；远离岸线的干地岩石不参与。

`addTidalWetness()` 沿用 Three.js r160 的 PBR 着色流程，不覆盖原始贴图。在原配色协调完成后调暗湿润部分，并降低粗糙度；保留法线、粗糙度贴图的细节，限制最低粗糙度，避免整块石头像塑料。实例的世界坐标必须先乘 `instanceMatrix` 再乘 `modelMatrix`，否则每块石头会使用同一条局部坐标水线。

扫描模型的 uniform 由 `CoastalProps` 持有，切画质只重新绑定场景，不重建模型材质。每帧在环境更新潮位后同步一个数值。备用程序岩石复用同一湿润函数，并直接共享沙地的潮位 uniform；高、低画质都有效。因此关闭素材请求时不会退回固定不动的湿润带。

这是实时外观近似：不计算岩石孔隙吸水、退潮后的慢干过程或逐波浪溅水。扩展 API 参考 [Three.js Material](https://threejs.org/docs/pages/Material.html)，实际 Shader 插入点以本项目锁定的 r160 源码为准。不能仅复制材质属性而丢掉 `onBeforeCompile`；缓存键也必须体现着色器变更。

### 草丛与器材避让

`CoastalProps` 保存加载模型变换后的包围盒，以及板架、沙滩巾的占地。`world.js` 的 `details.clearVegetation()` 根据草丛旋转/缩放后的几何包围盒，再留出风摆余量，检查整个草丛是否穿过物体，而不是只判断草根。

受影响实例退向后侧沙地，同时避开栈道和脚印路线。每次都从同一份种子矩阵开始计算，所以重复绑定不会越移越远，也不受六个模型的下载完成顺序影响。460/200 个草丛实例数量不变，不新增绘制批次。当前布局高档移动 2 丛，低档移动 1 丛；这种小范围修正不会把整片植被清空。

### 帆船与验证

远处帆船的基准高度现在包含潮位；轻摇使用 `waterTime`，波速为 0 时不再继续独自摇摆。它仍是视觉动画，不是基于船体采样的逐浪浮力求解。岸上的小木船、台球和 NPC 坐标不变。

运行 `npm run test:shore-detail`，或设置 `TIDELINE_URL=http://127.0.0.1:5175/` 后运行，检查当前成品而不是开发预览：

1. 桌面/手机实际截图；物体包围盒与带风摆余量的草丛无交叉，栈道草根数为 0。
2. 重复绑定的实例矩阵完全一致；高低画质切换仍只有 6 次模型请求。
3. 冻结水面时钟，涨退潮让船高相差 0.32，摇摆角度不变；扫描与备用岩石的潮位同步。
4. 冻结场景、隐藏海水和泡沫，仅改变真实 PBR 材质的湿润 uniform，再比较像素。湿润岩石必须实际变暗，且不能让整幅画面变色。不是只检查代码中出现了某个字符串。
5. 模拟模型 404，编译高低档备用岩石；恢复和释放场景后检查控制台错误。

截图为 `screenshots/shore-detail-*.png`。源码专项已通过；最终成品测试和交付校验在完成后记录于本节。

## 0.37：为什么上版仍有水面 bug

水面有动效、资源能释放，并不等于相机移动时的渲染正确。0.36 的测试没有检查“上一帧深度是否被清除”，因此遗漏了残影和三角形色块。

根因在 `OceanRefraction.capture()`：透明物体常使用 `depthWrite=false`，而 WebGL 的清除操作也受深度写入掩码影响。仅调用 `renderer.clear()` 会留下旧深度；下一视角的沙底和船受旧深度遮挡，颜色与深度不再配对。Three r160 自带 `Water.js` 的镜面过程已经显式开启深度写入，本项目折射过程缺少同一步。现在先执行 `renderer.state.buffers.depth.setMask(true)`，再清除并捕获。该内部 API 以项目锁定的 r160 为准，升级引擎需复测。

新增 `npm run test:water-layout`，不是仅检查 Shader 字符串：先渲染有内容的深度，强制关闭深度写入，再捕获空场景，断言整张采样深度被清空；随后切换三个相机位置，比对“从关闭掩码开始”与“从开启掩码开始”的深度样本完全一致。桌面、手机均运行，防止历史画面污染。

其他修正：

- 屏幕折射与反射偏移使用相机空间的法线扰动；平坦法线不应使倒影整体平移。屏幕边缘逐渐减弱折射偏移。
- 水下正弦点阵替换为低强度、不规则、随水深衰减的焦散线；使用屏幕导数缓和细线和高光闪烁。它仍是艺术近似，不是光线追踪。
- 泡沫地形顶点保持固定，用 `uWaterHeight` 抬高水下部分；湿沙和泡沫都以实际沙地高度与潮位差判断。不能再用“泡沫网格整体沿 Z 移动了多少”作为正确性标准。
- 水下坡面使用近岸密、远处疏的采样；手机纵向 16 段，桌面 24 段。减少看不见的远处细分，把预算留给近岸。

## 0.37：空间组织

不是增加一排互不相关的摆件，而是让已有物体按使用关系成组。软半写实配色沿用前版，原地形、小船 `(14,20)`、躺椅/棚 `(-13,27)`、台球 `(0,31.2)` 和 NPC 位置保持。

| 区域 | 调整 |
| --- | --- |
| 西侧休憩 | 第一张野餐桌从 `(-5,21)` 移至 `(-21,26)`，对应遮阳伞跟随；与原躺椅/棚形成休憩区域 |
| 中部活动 | 将挡视线的棕榈从 `(13,31)` 移到 `(-4,44)`；台球操作空间清除零散石子 |
| 东侧器材 | 冲浪板架集中到 `(38,24)` 并朝内转 `-0.22` 弧度，箱子和救生圈邻近布置；可见几何与碰撞体一起变换 |
| 连接路线 | `CoastalLayout.js` 描述栈道末端到活动区、休憩区的路线；增加一支脚印，并清理路线附近石子 |
| 手机 | 默认构图完整容纳船和球桌；球局面板默认收起，点折叠箭头或定位台球即可展开 |

核对不能只看俯视截图：新增测试检查器材包围盒中心与碰撞位置、船和球桌的投影边界、手机展开/收起、原球桌位置、涨退潮高度同步。截图命名 `screenshots/water-layout-*.png`。本版只重新安排已有布景，没有更改中八判罚、杆法或 NPC 任务流程。

## 本轮范围

0.37 最终成品验证：主文件 `index-Dbn7qAc-.js`，地址 `http://127.0.0.1:5175/`。水体/布局、NPC、外部资产、原建模、台球鼠标/触屏和完整场景测试均通过；控制台错误和布局溢出均为 0，跟踪资源完成释放。台球 18 个规则案例、60 次碰库、60 次袋角、30 次入袋及杆法/台呢测试仍通过。

注意测试也要匹配设计变化：脚印从 28 增至 70；手机新构图包含灯笼，不能沿用旧视角“省 0 次绘制”的断言。比较零透明度灯笼的画面差异时，要隐藏仍在播放 CSS 动画的 HUD，避免把 HTML 变化误判成水面变化。未降低原像素误差门槛。

以下保留 0.36 的水面与 NPC 设计说明。两位旧 Q 版角色依然只保留素材，运行的是两位 Quaternius 日常装游客。0.37 的布景调整以上文为准，中八规则与物理参数不改。

## 水面不是一张平面了

入口在 `src/experience/OceanWater.js`。保留 Three.js r160 `Water` 的镜像相机与裁剪，完整替换 vertex/fragment shader，不再依靠字符串给旧片元着色器叠加法线。

1. 距离分级网格在近岸密、远海疏；高档 128×96，低档 96×72。改变行方向后要翻转三角形索引，否则正面剔除会让整片海消失。
2. 四组 Gerstner 波叠加水平位移和高度。`k=2π/波长`，相位 `k·方向·位置−sqrt(gk)·时间`；法线由位移的两个偏导叉乘得到。
3. `oceanSpacing` 过滤网格采样不足的短波，避免远处折线；额外三层法线纹理补足小波纹，距离越远越弱。
4. 深度折射先隐藏海面、天空及无贡献的干岸细节，渲染海床颜色和深度，再恢复对象可见性与渲染器状态。保留岸石和沙底，增加向深处连续下降的水下坡面，不改岸上顶点。
5. 深度图转换回视空间距离，再减去水面距离，得到光穿过水的长度。根据前景深度拒绝错误偏移，避免把岸边物体拖入水里。
6. 透射率为 `exp(−吸收系数×水深)`；红光比蓝绿光更快衰减。Fresnel 的水面法向反射率取约 0.02037，越贴近海平线越偏向反射。
7. GGX 近似太阳高光；潮位改变海面高度，波速改变同一个水面时钟，海风影响细波强度。既有泡沫强度控制同步影响接触泡沫。

主画面、折射、镜面反射是三个绘制过程。完整帧统计不能只数最后一次 `renderer.render()`。折射目标最长边高档 960、低档 480，RGBA16F 颜色配深度；反射改为半浮点避免亮天空截断。尺寸变化复用目标，质量变化只更换水网格；释放时逐项验证只 dispose 一次。

这是实时图形近似，不是完整流体计算。没有水下相机模式、波浪破碎求解或物体浮力模拟；屏幕折射看不到当前相机画面之外的物体。不要将它描述为照片级或物理精确的海洋。

## NPC 状态与交互

`BeachNpcSystem.js` 负责 GLB、AnimationMixer、碰撞和角色状态，`src/ui/NpcDialog.js` 负责原生 dialog。模型包内已含资源，普通运行无需访问素材站。

| 角色 | 行为 | 对应代码 |
| --- | --- | --- |
| 小林 | 接取六处海玻璃记录，保留已有收集进度；定位下一处；集齐后确认完成 | `getConversation()`、`perform()` |
| 阿辰 | 聊天，定位台球区，不重开或修改当前球局 | `perform('pool')` → `focusBilliards()` |

工具栏“海滨相遇”可找到两人；直接点击/触摸角色也可对话。漫步视角锁定鼠标时，在 4.5 米内对准角色按 E。对话使用键盘焦点约束，Escape 关闭；打开对话取消击球瞄准并退出鼠标锁定，关闭后恢复适当的视角状态。

每个游客使用独立 AnimationMixer。Idle 循环，Victory 作为短暂招呼/庆祝，结束回到 Idle；保留 Walk 片段供后续扩展，本轮不做自主寻路。按照 Idle 的蒙皮后包围盒归一为约 1.9 米，而不是按静止骨架猜脚底位置。

点击检测采用当前姿态包围体并检查前景遮挡，避免动画中手脚之间的空隙导致触屏难点中。漫步碰撞是简化圆柱，不是逐三角形人体碰撞。

任务状态保存在当前页面内，刷新会重置；不会偷偷重置原来的拾光或球局。模型加载失败只影响游客，对话显示不可用，沙滩仍可进入。请求有超时/取消，质量切换不重新下载人物；卸载停止 mixer、释放骨架贴图/几何/材质并注销碰撞体。

## 素材来源与重建

作者：Quaternius。套件：[Ultimate Animated Character Pack](https://quaternius.com/packs/ultimatedanimatedcharacter.html)。作者标明 [CC0 1.0](https://creativecommons.org/publicdomain/zero/1.0/)，允许修改和再分发。原始下载来自作者页面链接的公开 Google Drive，并非官方 Three.js 模型，也不是用户提供的角色三视图建模结果。

使用 `Casual_Female.blend` 和 `Casual_Male.blend`。顶点配色改为低饱和日常服装，六种材质合为一个顶点色材质，保留 Idle/Walk/Victory，导出本地 GLB，总计 965368 字节。没有沿用源文件中的外部遗失纹理路径。来源、改动和 SHA256 见 `public/models/npc/manifest.json`。

```powershell
node scripts/fetch-npc-assets.mjs
& D:/blender.exe --background --disable-autoexec --python-exit-code 1 --python scripts/prepare-npc-assets.py -- Female
& D:/blender.exe --background --disable-autoexec --python-exit-code 1 --python scripts/prepare-npc-assets.py -- Male
npm run test:ocean-npc
npm run test:visual
```

Blender 路径可按电脑安装位置调整。作者源文件下载失败时无需重建，成品内的 GLB 仍可离线使用。重新导出后应重新核对 manifest 哈希，不能把旧哈希当作新文件校验结果。

## 0.36 验证记录（历史）

2026-09-11，在实际成品服务 `http://127.0.0.1:5175/` 上验证 0.36.0，主文件 `index-C65_CmMZ.js`。以下是固定测试视角的结果，不代表所有设备的帧率保证。

| 检查 | 结果 |
| --- | --- |
| 完整场景、昼夜与天气回归 | 桌面/手机均通过；控制台错误和布局溢出均为 0 |
| 完整帧开销，含水体额外绘制 | 桌面 128 次绘制、432532 三角形；手机 62 次、207281 三角形 |
| 水体真实内容 | GPU 深度存在近远差异；强涌浪两时刻约 12%/8% 的桌面/手机像素变化 |
| NPC 输入与任务 | 鼠标、触摸、附近 E、接取/提示/完成、台球定位均通过，球局不被重置 |
| 质量切换、错误隔离与释放 | NPC/碰撞体复用；GLB 404 不阻断沙滩；九项跟踪资源各释放一次 |
| 原有模型与台球 | 资产/场景建模/鼠标触屏回归通过；18 个规则案例、60 次碰库、60 次袋角、30 次入袋以及杆法/台泥测试通过 |

测试入口：`scripts/visual-check.mjs`、`scripts/ocean-npc-check.mjs`、`scripts/coastal-assets-check.mjs`、`scripts/scene-modeling-check.mjs`、`scripts/billiards-input-check.mjs`。截图在 `screenshots/`，例如 `ocean-npc-desktop-overview.png`、`ocean-mobile-waves.png` 和 `npc-mobile-dialog.png`。学习时应同时看代码、数值检查和截图，不能只看“通过”两个字。

## 学习练习

1. 固定时间、分别设涌浪 0 和 1，比较真实位移、反光和 GPU 完整帧统计。
2. 在低潮/高潮观察水下坡面的连续性；暂时显示深度图，理解距离不是屏幕颜色。
3. 检查 NPC 接任务前已收集的物品是否计数，完成记录是否会重复领取。
4. 打一杆台球后通过阿辰再次定位台球，核对局面、回合和杆数未变。
5. 切换高低画质、阻止一个 GLB 请求并刷新、打开后马上关闭页面，核对资源与错误隔离。

原理参考：[GPU Gems 水面模拟](https://developer.nvidia.com/gpugems/gpugems/part-i-natural-effects/chapter-1-effective-water-simulation-physical-models)、[Three.js DepthTexture](https://threejs.org/docs/pages/DepthTexture.html)、[AnimationMixer](https://threejs.org/docs/pages/AnimationMixer.html)。运行实现以项目锁定的 r160 本地源码为准。
